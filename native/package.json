{
  "name": "ssz-native",
  "version": "1.0.0",
  "description": "Native SHA-256 addon with Intel SHA-NI and ARM Crypto extensions",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "install": "node-gyp rebuild",
    "build": "node-gyp configure build",
    "clean": "node-gyp clean"
  },
  "dependencies": {
    "node-addon-api": "^7.0.0"
  },
  "devDependencies": {
    "node-gyp": "^10.0.0"
  }
}
